"""Castellan coordination layer: shared data models and (later) BoardState / convergence."""

from coordination.models import Finding, FindingClass, Severity

__all__ = ["Finding", "FindingClass", "Severity"]
