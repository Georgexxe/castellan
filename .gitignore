# Secrets — NEVER commit. Only the .example variants are tracked.
.env
agent_config.yaml

# LocalStack data volume
.localstack/

# M5 audit anchors (local out-of-band trusted heads)
.audit/

# Python / uv
.venv/
__pycache__/
*.pyc
*.pyo
.python-version

# OS / editor
.DS_Store
Thumbs.db
